------------------------------------------------------------------------------------------------------------------------------------------------------------------------ #

CREATE TABLE dsa_module_six.tb_vendas (
	 nome_funcionario VARCHAR(100) NOT NULL
    ,ano_fiscal INT NOT NULL
    ,valor_venda DECIMAL(18,2) NOT NULL
	,PRIMARY KEY (nome_funcionario, ano_fiscal)
);

------------------------------------------------------------------------------------------------------------------------------------------------------------------------ #